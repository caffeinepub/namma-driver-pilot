{
  "frontend/src/App.tsx": "Main TanStack Router app with full route tree; fixes the ProfileSetupModal usage by passing required `open` and `onComplete` props with local `profileSetupDone` state, and conditionally renders OfflineBanner based on backend health.",
  "frontend/src/hooks/useQueries.ts": "Centralized React Query hooks for all backend operations; updated `useGetMyRole` to use `normalizeRole()` from the new utility instead of direct `BackendRole` enum comparisons, ensuring both string and object-variant role formats from the actor are handled safely without crashing.",
  "frontend/src/hooks/useGetCallerUserProfile.ts": "Custom hook fetching the current user's profile via `(actor as any).getCallerUserProfile()` with proper loading states; imports `UserProfile` from the local types file.",
  "frontend/src/components/Layout.tsx": "Main layout wrapper component providing consistent header navigation, main content area, and footer with caffeine.ai attribution using lucide-react Heart icon.",
  "frontend/src/components/Navigation.tsx": "Responsive navigation header with role-aware dashboard link and role badge; role label is derived from the already-normalized role string returned by `useGetMyRole`, guarded so it only renders when role is a non-empty string (never blank or `[object Object]`).",
  "frontend/src/components/ProfileSetupModal.tsx": "First-time profile setup modal that calls `actor.setProfile()` directly with only `fullName` and `email` — never sends role; uses 30s timeout, logs errors to console, and shows toast on failure.",
  "frontend/src/pages/LandingPage.tsx": "Routing-only landing page at \"/\" that redirects based on auth/role state; fixes stale `/role-selection` navigate call to use the correct `/select-role` route path.",
  "frontend/src/pages/CustomerDashboard.tsx": "Customer dashboard that fetches trips via `useGetCustomerTrips` (filtered by caller's principal), maintains local `localTrips` state for optimistic updates, and passes `handleTripCreated` to `RideRequestForm` so newly booked trips appear instantly in the My Trips list without a refresh.",
  "frontend/src/pages/DriverDashboard.tsx": "Driver dashboard that wraps Available Trips and My Trips in a shadcn Tabs component inside a Card, replacing the previous stacked layout; removes any \"coming soon\" placeholder and provides clean tab switching between AvailableTripsSection and DriverTripList.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard with identity info card, stats, and three-tab interface; the role badge in the \"Logged-in Identity\" card now reads from `useGetMyRole()` (displaying \"Admin\" when the result is `#admin`) instead of the stored `userProfile.role` field.",
  "frontend/src/components/RideRequestForm.tsx": "Customer booking form that accepts an optional `onTripCreated` callback; after a successful `createTrip` mutation, converts the returned `BackendTrip` to the local `Trip` variant-object type and calls `onTripCreated` for optimistic UI updates in the parent.",
  "frontend/src/components/CustomerTripList.tsx": "Scrollable list of customer trips accepting `trips` and `isLoading` as props (no longer calls `useGetMyTrips` internally); renders status badges for all four statuses (pending/accepted/completed/cancelled) and vehicle type badges for each trip.",
  "frontend/src/components/AvailableTripsSection.tsx": "Driver-facing available trips list that uses `useGetAllTrips` (same hook as Admin) and filters client-side for trips with `status === '#requested'` and no driver assigned; shows an off-duty message when the driver's `isAvailable` is false, with no debug boxes.",
  "frontend/src/components/DriverTripList.tsx": "Driver's personal trip history list that uses `useGetAllTrips` and filters client-side for trips where `driverId` (typed as `[] | [Principal]`) matches the authenticated driver's principal; segments results into Current, Completed, and Other sections with all statuses shown, no debug boxes.",
  "frontend/src/components/AdminUsersTab.tsx": "Searchable table of all registered users; `getRoleLabel` and `getRoleBadgeVariant` now use `getRoleString()` from `lib/types` instead of the broken `'#customer' in roleVariant` pattern, eliminating the \"Cannot use 'in' operator\" crash.",
  "frontend/src/components/AdminTripsTab.tsx": "Admin trips table with search; imports `Trip` from local types file and uses variant-object pattern (`'#requested' in s`) to read status/type fields.",
  "frontend/src/index.css": "Global styles with OKLCH color system using warm earth tones (greens and ambers) for both light and dark modes, avoiding blue and purple.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom earth-tone color palette, Inter font family, and mobile-first responsive design tokens.",
  "frontend/src/pages/RoleSelectionPage.tsx": "Role selection page; all role comparisons use `getRoleString`, and `RoleSelectionWarningModal` is rendered with the required `open` and `onClose` props controlled by local `modalOpen` state.",
  "frontend/src/components/RoleSelectionWarningModal.tsx": "Role selection modal using explicit `useSetMyRoleCustomer`/`useSetMyRoleDriver` hooks instead of the broken `useSetMyRole`; shows toast errors on failure and never exposes admin as a selectable option.",
  "frontend/src/components/DriverProfileSection.tsx": "Read-only driver profile card that displays duty status, service area, vehicle experience, transmission comfort, and languages; imports `UserProfile` from `../lib/types` (not `../backend`) to match the type passed from `DriverDashboard`, and `DriverProfile` from `../backend` for the dedicated driver profile fields.",
  "frontend/src/components/EditDriverProfileModal.tsx": "Modal wrapper for the driver profile edit form; accepts `UserProfile | DriverProfile | null`, normalizes via `normalizeDriverProfile`, and passes the result to `EditDriverProfileForm`.",
  "frontend/src/components/EditDriverProfileForm.tsx": "Editable driver profile form that uses `useUpsertDriverProfile` mutation; shows \"Profile saved\" toast only on confirmed backend success, \"Save failed\" toast with `console.error` on failure, and closes the modal after a successful save.",
  "frontend/src/pages/AdminUpgradePage.tsx": "New hidden admin recovery page at `/admin/upgrade`; renders a centered card with title \"Admin Upgrade\", a password input for the setup code, and an \"Upgrade to Admin\" button that calls the backend and redirects to `/admin/dashboard` on success or shows the error message on failure.",
  "frontend/src/components/PricingTab.tsx": "Admin-only pricing configuration form that loads config from `useGetPricingConfig` on mount, populates all form fields from the fetched data, and saves via `useUpdatePricingConfig` mutation with \"Pricing saved\" toast on success and error toasts on failure.",
  "frontend/src/lib/types.ts": "Shared domain types with updated `RoleVariant` type covering both `{ admin: null }` (new) and `{ '#admin': null }` (legacy) formats; `getRoleString` now uses `normalizeRole` internally and handles optional-array wrappers, plain strings, and direct variant objects safely without any `in` operator crashes.",
  "frontend/src/utils/withTimeout.ts": "Utility function that wraps any Promise with a configurable timeout using Promise.race, rejecting with a TimeoutError if the timeout expires first.",
  "frontend/src/lib/defaultConfig.ts": "Exports a DEFAULT_CONFIG constant of type PricingConfig with all reasonable default pricing values, used as an immediate fallback when getPricingConfig fails or is still loading.",
  "frontend/src/components/ErrorBoundary.tsx": "React class-based Error Boundary that catches any unhandled render errors and displays a full-screen \"App failed to initialize\" recovery screen with Retry (reload) and Reset App (clear storage + reload) buttons.",
  "frontend/src/components/OfflineBanner.tsx": "Sticky top banner shown when the backend health check fails, displaying \"Backend is unreachable — some features may be unavailable\" with a dismiss button.",
  "frontend/src/components/DataLoadErrorBanner.tsx": "Dismissible red error banner displayed within dashboard content areas when any initial data query fails, showing \"Some data failed to load. Please refresh.\" with a refresh button.",
  "frontend/src/hooks/useBackendHealth.ts": "React Query hook that calls the backend `ping()` method on mount, logs success/failure with endpoint details to the console, and returns `isHealthy`, `isChecking`, and `error` states.",
  "frontend/src/pages/LoginPage.tsx": "New dedicated login page showing only the \"Namma Driver Pilot\" title and a \"Sign in with Internet Identity\" button; redirects to /post-login after successful authentication.",
  "frontend/src/pages/PostLoginLandingPage.tsx": "Post-login routing page that calls `useGetMyRole` and navigates to the correct dashboard using the normalized `roleKey` string (`admin`→`/admin/dashboard`, `driver`→`/driver/dashboard`, `customer`→`/customer/dashboard`, null→`/select-role`); shows spinner while loading and a retry card on error.",
  "frontend/src/components/AuthGate.tsx": "Auth gate component that waits up to 5 seconds for identity to resolve; shows a loading spinner while checking and redirects to /login if unauthenticated, rendering children only when authenticated.",
  "frontend/src/components/ProtectedRoute.tsx": "Lightweight route guard that immediately redirects to /login if the user is not authenticated, rendering children only when identity is confirmed.",
  "frontend/src/pages/SelectRolePage.tsx": "Role selection page showing only \"I am a Customer\" and \"I am a Driver\" options; uses explicit `useSetMyRoleCustomer`/`useSetMyRoleDriver` hooks, redirects to correct dashboard on success, and shows toast errors on failure.",
  "frontend/src/pages/NotAuthorizedPage.tsx": "Simple centered page with a ShieldX icon, \"Not Authorized\" heading, explanation text, and a \"Go Home\" button; renders no sensitive data.",
  "frontend/src/components/RoleGuard.tsx": "Role-based route guard using `useGetMyRole`; for admin route mismatches shows a toast \"Admin only\" before rendering `NotAuthorizedPage`, and redirects non-admin/non-matching roles to `/select-role` without crashing.",
  "frontend/src/utils/normalizeProfile.ts": "Utility module exporting `NormalizedDriverProfile` type, `normalizeArrayField` helper, and `normalizeDriverProfile` function that converts Candid variant object arrays and `[] | [string[]]` optional languages into plain `string[]` fields without TypeScript intersection type conflicts.",
  "frontend/src/utils/normalizeRole.ts": "Utility that safely converts any role value (plain string, Candid variant object `{ admin: null }`, legacy `{ '#admin': null }`, or null/undefined) into a normalized role string without `#` prefix, returning `''` as a safe fallback."
}