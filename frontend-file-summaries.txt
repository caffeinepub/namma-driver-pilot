{
  "frontend/src/App.tsx": "Main TanStack Router app with full route tree; `AppShell` passes `onClose` (not `onComplete`) to `ProfileSetupModal`, matching the updated prop interface, and uses `RoleGuard` with `allowedRole` prop for protected routes.",
  "frontend/src/hooks/useQueries.ts": "Centralized React Query hooks; `useGetMyRole` derives `identityReady` locally as `!isInitializing` (from the existing `isInitializing` field on `InternetIdentityContext`) instead of accessing the non-existent `identityReady` property. All other hooks are unchanged.",
  "frontend/src/hooks/useGetCallerUserProfile.ts": "Custom hook fetching the current user's profile via `(actor as any).getCallerUserProfile()` with proper loading states; imports `UserProfile` from the local types file.",
  "frontend/src/components/Layout.tsx": "Main layout wrapper component providing consistent header navigation, main content area, and footer with caffeine.ai attribution using lucide-react Heart icon.",
  "frontend/src/components/Navigation.tsx": "Responsive navigation header using `data` (not `role`) from `useGetMyRole` and comparing against the `Role` enum from the backend interface for role-aware dashboard links and badges.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-time profile setup modal that converts any role value (enum string or Candid variant object) to a proper Candid variant object via `roleToVariant` before calling `saveCallerUserProfile`, preventing the \"in operator on string\" crash; uses `unknown` typing for `rawRole` to avoid TypeScript inference errors.",
  "frontend/src/pages/LandingPage.tsx": "Routing-only landing page that redirects unauthenticated users to `/login` and authenticated users to their role-appropriate dashboard via `useGetMyRole`, with `/select-role` as the fallback for unassigned users.",
  "frontend/src/pages/CustomerDashboard.tsx": "Customer dashboard that fetches real backend trips via `useGetCustomerTrips` and handles optimistic trip creation; fixes the type mismatch by accepting the local `Trip` type from `RideRequestForm` (which already converts `BackendTrip` → local `Trip`) and normalizing it to `NormalizedTrip` via `localTripToNormalized` before adding to optimistic state.",
  "frontend/src/pages/DriverDashboard.tsx": "Driver dashboard that wraps Available Trips and My Trips in a shadcn Tabs component inside a Card, replacing the previous stacked layout; removes any \"coming soon\" placeholder and provides clean tab switching between AvailableTripsSection and DriverTripList.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard; fixes import to use `useGetAllTripsAdmin` instead of the non-existent `useGetAllTrips`.",
  "frontend/src/components/RideRequestForm.tsx": "Customer booking form with full One Way / Round Trip logic — One Way shows a \"Drop Location Mode\" toggle (Manual/GPS) matching the Pickup toggle UX, with GPS capture storing drop lat/lng and editable Drop Area/Pincode fields; Round Trip hides all drop inputs and shows a helper text, auto-syncing drop state to pickup via useEffect, and submits dropoffLocation = pickupLocation. The shared `captureGps` helper is reused for both pickup and drop GPS capture.",
  "frontend/src/components/CustomerTripList.tsx": "Scrollable customer trip list updated to accept `NormalizedTrip[]` instead of the old variant-object `Trip[]`; all `in` operator usage removed, status/vehicleType comparisons use plain `===` string equality on normalized values.",
  "frontend/src/components/AvailableTripsSection.tsx": "Driver-facing available trips list that normalizes all backend trips through `normalizeTrip()` before rendering; all `in` operator checks removed and replaced with plain `===` string comparisons on normalized `vehicleType` and `tripType` fields using `NormalizedTrip` type throughout.",
  "frontend/src/components/DriverTripList.tsx": "Driver's personal trip history list now uses `useGetMyDriverTrips` instead of `useGetAllTrips`; all `in` operator usage removed and replaced with plain `===` string comparisons on normalized status; uses `NormalizedTrip` type throughout.",
  "frontend/src/components/AdminUsersTab.tsx": "Searchable admin users table; now imports `UserProfile` directly from `../backend` (not the local types file) so `getRoleBadgeVariant` and `getRoleLabel` receive the correct backend type without a `languages` field mismatch.",
  "frontend/src/components/AdminTripsTab.tsx": "Admin trips table using `useGetAllTripsAdmin` with normalized trip data; fixes the `dropoffLocation` null/undefined check by using `== null` (covers both null and undefined) in both the filter and the `formatDropoff` helper.",
  "frontend/src/index.css": "Global styles with OKLCH color system using warm earth tones (greens and ambers) for both light and dark modes, avoiding blue and purple.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom earth-tone color palette, Inter font family, and mobile-first responsive design tokens.",
  "frontend/src/pages/RoleSelectionPage.tsx": "Legacy role selection page (previously an empty file) now exports a full component matching `SelectRolePage`; kept as a valid module to satisfy the `App.tsx` import.",
  "frontend/src/components/RoleSelectionWarningModal.tsx": "Role selection modal using `data` (not `role`) from `useGetMyRole` and comparing against the `Role` enum; calls `useSetMyRoleCustomer`/`useSetMyRoleDriver` which correctly invoke `actor.setMyRole()`.",
  "frontend/src/components/DriverProfileSection.tsx": "Read-only driver profile card; now imports both `DriverProfile` and `UserProfile` from `../backend` directly, eliminating the `languages` type incompatibility with the local `UserProfile` type.",
  "frontend/src/components/EditDriverProfileModal.tsx": "Modal wrapper for the driver profile edit form; accepts `UserProfile | DriverProfile | null`, normalizes via `normalizeDriverProfile`, and passes the result to `EditDriverProfileForm`.",
  "frontend/src/components/EditDriverProfileForm.tsx": "Editable driver profile form that uses `useUpsertDriverProfile` mutation; shows \"Profile saved\" toast only on confirmed backend success, \"Save failed\" toast with `console.error` on failure, and closes the modal after a successful save.",
  "frontend/src/pages/AdminUpgradePage.tsx": "New hidden admin recovery page at `/admin/upgrade`; renders a centered card with title \"Admin Upgrade\", a password input for the setup code, and an \"Upgrade to Admin\" button that calls the backend and redirects to `/admin/dashboard` on success or shows the error message on failure.",
  "frontend/src/components/PricingTab.tsx": "Admin pricing configuration form; `handleSave` now uses plain try/catch instead of checking `__kind__` on the already-unwrapped `PricingConfig` return value from `useUpdatePricingConfig`.",
  "frontend/src/lib/types.ts": "Shared domain types with role utilities (`normalizeRole`, `getRoleString`, `getRoleLabel`, `getRoleBadgeVariant`) and all Trip/UserProfile/Location types; `UserProfile.languages` is now typed as `string[] | null | undefined` to be compatible with both the backend type and Candid optional formats.",
  "frontend/src/utils/withTimeout.ts": "Utility function that wraps any Promise with a configurable timeout using Promise.race, rejecting with a TimeoutError if the timeout expires first.",
  "frontend/src/lib/defaultConfig.ts": "Exports a DEFAULT_CONFIG constant of type PricingConfig with all reasonable default pricing values, used as an immediate fallback when getPricingConfig fails or is still loading.",
  "frontend/src/components/ErrorBoundary.tsx": "React class-based Error Boundary that catches any unhandled render errors and displays a full-screen \"App failed to initialize\" recovery screen with Retry (reload) and Reset App (clear storage + reload) buttons.",
  "frontend/src/components/OfflineBanner.tsx": "Sticky top banner shown when the backend health check fails, displaying \"Backend is unreachable — some features may be unavailable\" with a dismiss button.",
  "frontend/src/components/DataLoadErrorBanner.tsx": "Dismissible red error banner displayed within dashboard content areas when any initial data query fails, showing \"Some data failed to load. Please refresh.\" with a refresh button.",
  "frontend/src/hooks/useBackendHealth.ts": "React Query hook that calls the backend `ping()` method on mount, logs success/failure with endpoint details to the console, and returns `isHealthy`, `isChecking`, and `error` states.",
  "frontend/src/pages/LoginPage.tsx": "New dedicated login page showing only the \"Namma Driver Pilot\" title and a \"Sign in with Internet Identity\" button; redirects to /post-login after successful authentication.",
  "frontend/src/pages/PostLoginLandingPage.tsx": "Post-login routing page that waits for `isFetched && !isLoading && !isFetching` before executing role-based navigation; uses a `useRef` guard to prevent repeated redirects; shows spinner while loading and error panel on failure.",
  "frontend/src/components/AuthGate.tsx": "Auth gate that blocks all route rendering until the identity session is resolved (`!isInitializing`) and the `myRole` query is fetched; derives `identityReady` locally as `!isInitializing` instead of reading a non-existent property from the hook type. Includes a 9-second timeout with a clean error panel and single Retry button.",
  "frontend/src/components/ProtectedRoute.tsx": "Lightweight route guard that immediately redirects to /login if the user is not authenticated, rendering children only when identity is confirmed.",
  "frontend/src/pages/SelectRolePage.tsx": "Role selection page that waits for `roleFetched && !roleLoading` before executing any redirect; uses a `useRef` guard to prevent double redirects; UI and role-setting logic unchanged.",
  "frontend/src/pages/NotAuthorizedPage.tsx": "Simple centered page with a ShieldX icon, \"Not Authorized\" heading, explanation text, and a \"Go Home\" button; renders no sensitive data.",
  "frontend/src/components/RoleGuard.tsx": "Role-based route guard using `useGetMyRole` with the `Role` enum; redirects unassigned users to `/select-role`, shows `NotAuthorizedPage` for role mismatches, and handles all four Role variants correctly.",
  "frontend/src/utils/normalizeProfile.ts": "Utility module exporting `NormalizedDriverProfile` type, `normalizeArrayField` helper, and `normalizeDriverProfile` function that converts Candid variant object arrays and `[] | [string[]]` optional languages into plain `string[]` fields without TypeScript intersection type conflicts.",
  "frontend/src/utils/normalizeRole.ts": "Utility that safely converts any role value (plain string, Candid variant object `{ admin: null }`, legacy `{ '#admin': null }`, or null/undefined) into a normalized role string without `#` prefix, returning `''` as a safe fallback.",
  "frontend/src/utils/normalizeTrip.ts": "New utility that converts raw backend `Trip` objects (with Candid enum strings, hash-prefixed strings, or variant objects) into a `NormalizedTrip` with plain string fields for status, tripType, journeyType, and vehicleType — safe for `===` comparisons without the `in` operator.",
  "frontend/src/hooks/useInternetIdentity.ts": "Internet Identity provider hook with `identityReady: boolean` added to both the `InternetIdentityContext` type definition and the memoized provider value; `identityReady` is `true` when `loginStatus !== 'initializing'` (session resolved as logged-in or logged-out). No actor-related state is present here."
}