{
  "frontend/src/App.tsx": "Main app with TanStack Router and role-based route guards; adds the new `/admin/upgrade` route (`adminUpgradeRoute`) with a login-only guard (no role restriction) that renders `AdminUpgradePage`, while all existing routes and guards remain unchanged.",
  "frontend/src/hooks/useQueries.ts": "Centralized React Query hooks for all backend operations; adds `useUpdateAvailability` mutation that calls `actor.updateAvailability(isAvailable)` and invalidates the user profile cache, and also invalidates `currentUserProfile` on `useCompleteTrip` success so the availability toggle reflects the latest saved value after a trip is completed.",
  "frontend/src/hooks/useGetCallerUserProfile.ts": "Custom hook fetching the current user's profile with proper loading states to prevent modal flash; logs the role value (now `?AppRole`, no LockedRole wrapper) on every successful fetch.",
  "frontend/src/components/Layout.tsx": "Main layout wrapper component providing consistent header navigation, main content area, and footer with caffeine.ai attribution using lucide-react Heart icon.",
  "frontend/src/components/Navigation.tsx": "Responsive navigation header with user dropdown; updated to use `userProfile.role` directly as `AppRole` string (no more `.role.role` / `.role.isLocked` LockedRole accessors).",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal for first-time user profile setup (email + full name); saves profile with `role: undefined` so no role is assigned at this stage — role selection happens separately on the Role Selection screen.",
  "frontend/src/pages/LandingPage.tsx": "Landing page with post-login routing; uses `userProfile.role == null` guard (new `?AppRole` type) to route users without a role to role selection, and users with a role directly to their dashboard.",
  "frontend/src/pages/CustomerDashboard.tsx": "Customer dashboard displaying ride request form and personal trip list in a two-column responsive layout.",
  "frontend/src/pages/DriverDashboard.tsx": "Driver dashboard with tabbed interface showing Available Trips and My Trips tabs; the My Trips card description dynamically reflects whether an active trip is in progress and the total trip count.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard with identity info card (name, email, role badge, principal ID), stats row (total users/trips), and tabbed interface for all trips and all users with search functionality.",
  "frontend/src/components/RideRequestForm.tsx": "Customer booking form with GPS mode visibility fix — Pickup Pincode and Pickup Area fields are conditionally rendered only when `locationMode === 'manual'`; when GPS permission is denied, `locationMode` auto-reverts to `'manual'`, a `gpsError` state is set, and the error message \"Location permission denied. Please enter pickup manually.\" is displayed via an Alert component near the location mode radio buttons; the error clears when the user manually switches location mode or on form reset.",
  "frontend/src/components/CustomerTripList.tsx": "Scrollable list displaying customer's trips with formatted pickup/drop locations (area, pincode), color-coded status badges, and timestamps.",
  "frontend/src/components/AvailableTripsSection.tsx": "Scrollable list of requested trips for drivers to accept, displaying formatted pickup/drop locations (area, pincode), trip details, and accept buttons.",
  "frontend/src/components/DriverTripList.tsx": "Refactored driver trip list that segments trips into three labeled sections — Current Trip (accepted, with \"Mark as Completed\" button), Completed Trips (sorted newest-first, no action buttons), and Other (cancelled/rejected, hidden when empty) — each with color-coded status badges showing human-readable labels (Pending/Accepted/Completed/Cancelled).",
  "frontend/src/components/AdminUsersTab.tsx": "Searchable table displaying all registered users with email, name, role badges (using `user.role` directly as `?AppRole`), and join dates; handles null/undefined role gracefully.",
  "frontend/src/components/AdminTripsTab.tsx": "Admin trips table with search, showing columns for Customer, Driver, Pickup, Dropoff, Time, and a color-coded Status badge using human-readable labels (Pending/Accepted/Completed/Cancelled); display-only with no action buttons.",
  "frontend/src/index.css": "Global styles with OKLCH color system using warm earth tones (greens and ambers) for both light and dark modes, avoiding blue and purple.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom earth-tone color palette, Inter font family, and mobile-first responsive design tokens.",
  "frontend/src/pages/RoleSelectionPage.tsx": "Role selection page showing three role cards (Customer, Driver, Admin); uses `userProfile.role == null` guard (new `?AppRole` type) and shows the RoleSelectionWarningModal for users without a saved role.",
  "frontend/src/components/RoleSelectionWarningModal.tsx": "Role selection modal with three options (Customer, Driver, Setup Admin); Admin path reveals a code input field and validates against hardcoded `NAMMA5600`, showing \"Invalid admin setup code\" on mismatch without saving the role.",
  "frontend/src/components/DriverProfileSection.tsx": "Read-only driver profile display card showing service area, vehicle experience, earnings, and duty status; displays \"On-Duty\"/\"Off-Duty\" badges and shows \"Duty status is locked while you have an active trip.\" lock message when `hasAcceptedTrip` is true.",
  "frontend/src/components/EditDriverProfileModal.tsx": "Modal dialog wrapper for the driver profile edit form; updated description text to reference \"duty status settings\" and forwards the `hasAcceptedTrip` boolean prop to `EditDriverProfileForm`.",
  "frontend/src/components/EditDriverProfileForm.tsx": "Editable driver profile form with service area, vehicle/transmission/language checkboxes, and a Duty Status toggle that shows \"On-Duty\"/\"Off-Duty\" labels; when `hasAcceptedTrip` is true the toggle is locked with the message \"Duty status is locked while you have an active trip.\"",
  "frontend/src/pages/AdminUpgradePage.tsx": "New hidden admin recovery page at `/admin/upgrade`; renders a centered card with title \"Admin Upgrade\", a password input for the setup code, and an \"Upgrade to Admin\" button that calls the backend and redirects to `/admin/dashboard` on success or shows the error message on failure."
}