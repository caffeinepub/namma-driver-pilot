{
  "frontend/src/App.tsx": "Main app with TanStack Router; replaces old role-selection route with /select-role (SelectRolePage), wraps /admin/* with RoleGuard(admin, showNotAuthorized), /customer/* with RoleGuard(customer), /driver/* with RoleGuard(driver), and adds /not-authorized route.",
  "frontend/src/hooks/useQueries.ts": "Centralized React Query hooks for all backend operations; adds `convertRoleToVariant` helper that maps plain role strings (`'customer'`/`'driver'`) to Candid variant objects (`{ customer: null }`/`{ driver: null }`), and applies this conversion in both `useSetMyRole` and `useUpdateUserRole` before calling `actor.setMyRole`, fixing the Candid variant encoding mismatch.",
  "frontend/src/hooks/useGetCallerUserProfile.ts": "Custom hook fetching the current user's profile via `(actor as any).getCallerUserProfile()` with proper loading states; imports `UserProfile` from the local types file.",
  "frontend/src/components/Layout.tsx": "Main layout wrapper component providing consistent header navigation, main content area, and footer with caffeine.ai attribution using lucide-react Heart icon.",
  "frontend/src/components/Navigation.tsx": "Responsive navigation header; now uses useGetMyRole to conditionally show \"Admin Dashboard\" link only for admin role users, and shows generic \"Dashboard\" link for customer/driver roles.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal for first-time user profile setup (email + full name); imports `UserProfile` from local types file and uses `useSaveCallerUserProfile` mutation.",
  "frontend/src/pages/LandingPage.tsx": "Routing-only landing page at \"/\" that redirects based on auth/role state; fixes stale `/role-selection` navigate call to use the correct `/select-role` route path.",
  "frontend/src/pages/CustomerDashboard.tsx": "Customer dashboard with ride request form and trip list; adds principal ID info card, Role badge, and a disabled \"Create Booking\" placeholder button at the top.",
  "frontend/src/pages/DriverDashboard.tsx": "Driver dashboard with profile section, available trips, and my trips tabs; adds principal ID info card, Role badge, and a disabled \"View Assigned Trips\" placeholder button at the top.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard with identity info, stats, and three-tab interface; now shows DataLoadErrorBanner when any query (users, trips, profile) fails, rendering with fallback empty data rather than blocking.",
  "frontend/src/components/RideRequestForm.tsx": "Customer booking form that constructs a complete `TripRequest` object using `TripType` and `JourneyType` enums from the backend, with `vehicleType` cast via `as any` since `VehicleType` is not exported from the backend interface; passes the single object to `useCreateTrip` with friendly error handling and form reset on success.",
  "frontend/src/components/CustomerTripList.tsx": "Scrollable list of customer's trips with status badges and fare display; imports `Trip` from local types file and uses variant-object pattern for status/vehicle fields.",
  "frontend/src/components/AvailableTripsSection.tsx": "Scrollable list of available trips for drivers to accept; imports `Trip` from local types file and uses variant-object pattern for all discriminated union fields.",
  "frontend/src/components/DriverTripList.tsx": "Driver trip list segmented into Current/Completed/Other sections; imports `Trip` from local types file and uses variant-object pattern for all discriminated union fields.",
  "frontend/src/components/AdminUsersTab.tsx": "Searchable table of all registered users; imports `UserProfile` from local types file and uses variant-object pattern to read role/status fields.",
  "frontend/src/components/AdminTripsTab.tsx": "Admin trips table with search; imports `Trip` from local types file and uses variant-object pattern (`'#requested' in s`) to read status/type fields.",
  "frontend/src/index.css": "Global styles with OKLCH color system using warm earth tones (greens and ambers) for both light and dark modes, avoiding blue and purple.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom earth-tone color palette, Inter font family, and mobile-first responsive design tokens.",
  "frontend/src/pages/RoleSelectionPage.tsx": "Role selection page; all role comparisons use `getRoleString`, and `RoleSelectionWarningModal` is rendered with the required `open` and `onClose` props controlled by local `modalOpen` state.",
  "frontend/src/components/RoleSelectionWarningModal.tsx": "Role selection modal with Customer/Driver options; updated to use `useSetMyRole` (instead of the broken `useUpdateUserRole`) so role submission uses the correct Candid variant object encoding via the shared `convertRoleToVariant` helper.",
  "frontend/src/components/DriverProfileSection.tsx": "Read-only driver profile display card; imports `UserProfile` from local types file and reads vehicle/transmission experience via variant-object pattern.",
  "frontend/src/components/EditDriverProfileModal.tsx": "Modal wrapper for the driver profile edit form; imports `UserProfile` from local types file.",
  "frontend/src/components/EditDriverProfileForm.tsx": "Editable driver profile form with service area, vehicle/transmission/language checkboxes, and duty status toggle; imports `UserProfile` from local types file.",
  "frontend/src/pages/AdminUpgradePage.tsx": "New hidden admin recovery page at `/admin/upgrade`; renders a centered card with title \"Admin Upgrade\", a password input for the setup code, and an \"Upgrade to Admin\" button that calls the backend and redirects to `/admin/dashboard` on success or shows the error message on failure.",
  "frontend/src/components/PricingTab.tsx": "Admin-only pricing configuration form component with four grouped sections (Local, Outstation + KM Slabs, Vehicle Multipliers, Commission); loads config via `useGetPricingConfig`, checks admin status via `useCheckIsAdmin`, shows an Unauthorized screen for non-admins, and saves via `useUpdatePricingConfig` with success toast and inline error handling.",
  "frontend/src/lib/types.ts": "Shared domain types file; adds `RoleVariant` type alias and exports a `getRoleString` helper that safely extracts a plain `AppRole` string from the Candid optional variant `[] | [RoleVariant]` used in `UserProfile.role`.",
  "frontend/src/utils/withTimeout.ts": "Utility function that wraps any Promise with a configurable timeout using Promise.race, rejecting with a TimeoutError if the timeout expires first.",
  "frontend/src/lib/defaultConfig.ts": "Exports a DEFAULT_CONFIG constant of type PricingConfig with all reasonable default pricing values, used as an immediate fallback when getPricingConfig fails or is still loading.",
  "frontend/src/components/ErrorBoundary.tsx": "React class-based Error Boundary that catches any unhandled render errors and displays a full-screen \"App failed to initialize\" recovery screen with Retry (reload) and Reset App (clear storage + reload) buttons.",
  "frontend/src/components/OfflineBanner.tsx": "Sticky top banner shown when the backend health check fails, displaying \"Backend is unreachable — some features may be unavailable\" with a dismiss button.",
  "frontend/src/components/DataLoadErrorBanner.tsx": "Dismissible red error banner displayed within dashboard content areas when any initial data query fails, showing \"Some data failed to load. Please refresh.\" with a refresh button.",
  "frontend/src/hooks/useBackendHealth.ts": "React Query hook that calls the backend health() method on mount with a 10-second timeout, returning isHealthy, isChecking, and error states without blocking rendering.",
  "frontend/src/pages/LoginPage.tsx": "New dedicated login page showing only the \"Namma Driver Pilot\" title and a \"Sign in with Internet Identity\" button; redirects to /post-login after successful authentication.",
  "frontend/src/pages/PostLoginLandingPage.tsx": "Post-login page that calls `useGetMyRole` and redirects to the correct dashboard based on role (admin→/admin/dashboard, customer→/customer/dashboard, driver→/driver/dashboard, null→/select-role); shows spinner while loading and a retry card on error.",
  "frontend/src/components/AuthGate.tsx": "Auth gate component that waits up to 5 seconds for identity to resolve; shows a loading spinner while checking and redirects to /login if unauthenticated, rendering children only when authenticated.",
  "frontend/src/components/ProtectedRoute.tsx": "Lightweight route guard that immediately redirects to /login if the user is not authenticated, rendering children only when identity is confirmed.",
  "frontend/src/pages/SelectRolePage.tsx": "New /select-role page showing the authenticated principal ID and two large role-selection buttons (\"I am a Customer\" / \"I am a Driver\"); calls setMyRole on click and redirects to the correct dashboard, with auto-redirect if role already exists and a Logout button.",
  "frontend/src/pages/NotAuthorizedPage.tsx": "Simple centered page with a ShieldX icon, \"Not Authorized\" heading, explanation text, and a \"Go Home\" button; renders no sensitive data.",
  "frontend/src/components/RoleGuard.tsx": "Role-based route guard that verifies the user's role via useGetMyRole; shows spinner while loading, retry UI on error, NotAuthorizedPage for admin route mismatches (showNotAuthorized=true), and redirects to /select-role for customer/driver mismatches."
}